#include <stdio.h>
#include <string.h>
int reverse()

{

int r, i, c;
	c = 0;
	printf("Enter number of elements in the array :");
	scanf("%d", &r);
	char a[r], n, o;
	char b[r];

}

int main()
{
    int r, i, c;
	c = 0;
	printf("Enter number of elements in the array :");
	scanf("%d", &r);
	char a[r], n, o;
	reverse();
}
